[Unit]
Description=tteatro: refresh ticket links daily

[Timer]
OnCalendar=*-*-* 05:00:00
# If the droplet was off at a scheduled time, run as soon as it's back.
Persistent=true

[Install]
WantedBy=timers.target
